<!--
Kimi 适配 7 原则（本模板适用）：
1. 结构显式化：方法型 Skill 严格六段式，工具链型 Skill 严格安装/配置/使用/排错四段
2. 指令直接化：禁止模糊动词，用「必须 / 禁止 / 如果…则…」
3. 判断标准外化：所有评分、决策写成 if-then 或数值阈值
4. 上下文自包含：关键引用使用完整 vault 路径，单文件可独立执行
5. 输出 schema 化：规定输出字段、顺序、数据类型
6. 示例驱动：每个复杂输出配 1 个最小真实示例，禁止编造数据
7. 单文件 ≤150 行：超过则拆分为子 Skill 或 Workflow
-->

# {Skill 中文名}

## 使用场景

- {场景 1}
- {场景 2}

## 输入格式

### 必须输入
| 字段 | 类型 | 说明 |
|------|------|------|
| {input} | {type} | {说明} |

### 可选输入
| 字段 | 类型 | 说明 |
|------|------|------|
| {input} | {type} | {说明} |

## 执行流程

1. **{步骤名}**：{做什么}
2. **{步骤名}**：{做什么}
3. **{步骤名}**：{做什么}

### 判断标准（if-then）
- 如果 {条件 A}，则 {动作 A}
- 如果 {条件 B}，则 {动作 B}
- 如果无法判断，则 {默认动作}

## 输出格式

### 必须字段
| 字段 | 类型 | 说明 |
|------|------|------|
| {output} | {type} | {说明} |

### 输出示例
```markdown
{最小真实示例，禁止编造评论/数据/用户反馈}
```

## 示例

### 示例 1：{场景标签}
**输入**：
```markdown
{输入示例}
```

**输出**：
```markdown
{输出示例}
```

---

# 工具链型 Skill 规范（当本 Skill 是脚本/发布/渲染工具时使用）

## 1. 安装

```bash
{安装命令}
```

## 2. 配置

- {配置项 1}
- {配置项 2}

## 3. 使用

```bash
{使用命令}
```

## 4. 排错

| 现象 | 原因 | 解决 |
|------|------|------|
| {现象} | {原因} | {解决} |

---

> **版本**：v{M.N}  
> **最后更新**：{YYYY-MM-DD}  
> **所属 Agent**：[[agents/{Agent名}.md|{中文名} Agent]]
